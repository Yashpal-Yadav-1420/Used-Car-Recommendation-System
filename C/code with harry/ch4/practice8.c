/*write a program to check wether the number is prime or not using loops.*/
#include <stdio.h>

int main(){
    int i = 2 , n, prime = 0;
    while (i<=n)
    {
         i++;    
    }
    if (n%i == 0 )
        {
            printf("This number is prime %d",n);
        }
        else
        {
            printf("This number is nit prime %d", n);
        }


    return 0;
}